# 모바일 겹침 현상 1차 확인

2026-05-10 모바일 폭 390px 기준으로 메인과 상세 페이지를 확인했다.

## 확인 결과

메인 페이지에서는 HERO 하단의 추상 비주얼 다음에 통계 카드 영역이 이어지는 구간에서 `2018` 카드 이후 다음 통계 카드들이 매우 희미하게 겹쳐 보이거나 공간이 과도하게 무너진 것처럼 나타난다. 이후 `사업분야` 섹션 제목이 하단으로 이어지며, 일부 큰 제목이 모바일 폭에서 잘려 보이는 느낌도 있다.

상세 페이지 `/business/data-operation`에서는 상단 상세 카드 안의 어두운 프로세스 박스가 오른쪽으로 넘쳐 보인다. 이로 인해 카드 내부 콘텐츠가 모바일 폭을 넘어가고, 스크롤 중 상하 섹션이 겹치는 것처럼 보일 가능성이 높다.

## 의심 원인

1. 모바일에서 `reveal-up` 애니메이션과 blur/backdrop 효과가 많은 카드에 적용되어, 첫 렌더링 또는 스크롤 중 opacity/transform 상태가 콘텐츠 겹침처럼 보일 수 있다.
2. 메인 통계 카드 영역의 큰 숫자와 카드 높이가 모바일에서 안정적으로 확보되지 않아, 다음 요소가 흐릿하게 겹쳐 보인다.
3. 상세 페이지의 어두운 운영 구조 박스 또는 내부 grid/card 요소가 모바일에서 `min-width` 또는 폭 제약 없이 렌더링되어 오른쪽으로 overflow된다.
4. 일부 sticky 레이아웃은 `lg:` 없이 기본 적용된 구간이 있어 모바일에서 영향 가능성을 점검해야 한다.

## 수정 방향

모바일 전용으로 reveal 애니메이션을 정지하거나 transform을 제거하고, 주요 섹션과 카드에 명시적인 overflow 제어와 충분한 간격을 부여한다. 상세 페이지의 내부 박스는 `overflow-hidden`, `min-w-0`, 모바일 단일 컬럼, 줄바꿈을 강제해 가로 넘침과 시각적 겹침을 방지한다.


## 2026-05-10 추가 확인

모바일 전체 스크린샷 재검증 결과, 섹션 겹침은 대부분 해소됐지만 390px 폭에서 메인 HERO 대형 헤드라인과 일부 상세 페이지 긴 문단이 오른쪽으로 잘리는 현상이 남아 있었다. 이는 세로 겹침보다는 `body`/루트의 `overflow-x: hidden`으로 증상이 가려진 상태에서 텍스트 자체가 안전하게 줄바꿈되지 않는 문제로 판단된다. 다음 수정에서는 모바일 전용으로 HERO 헤드라인 크기를 한 단계 더 낮추고, 주요 제목·문단·카드 영역의 `overflow-wrap:anywhere`, `word-break:keep-all`, `max-width:100%`와 컨테이너 폭을 더 보수적으로 적용한다.


## 2026-05-10 final2 추가 확인

390px 모바일 뷰포트에서 메인 HERO의 큰 겹침은 사라졌지만, 메인 보조 문장 말미와 사업 상세 페이지 본문 일부가 오른쪽에서 잘리는 현상이 남아 있음을 확인했다. 원인은 모바일에서 `.container`에 폭 제한과 좌우 padding이 동시에 적용되어 실제 박스 총폭이 뷰포트를 초과하는 구조로 판단된다. 따라서 모바일에서는 `.container`를 `box-sizing: border-box`, `width: 100%`, `max-width: 100%`로 고정하고, HERO 보조 문장은 모바일에서 자연스럽게 두 줄로 분리해 텍스트가 카드 경계 밖으로 밀리지 않도록 보완한다.
